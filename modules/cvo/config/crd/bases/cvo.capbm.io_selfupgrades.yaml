---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: selfupgrades.cvo.capbm.io
spec:
  group: cvo.capbm.io
  names:
    kind: SelfUpgrade
    listKind: SelfUpgradeList
    plural: selfupgrades
    singular: selfupgrade
  scope: Namespaced
  versions:
  - name: v1beta2
    schema:
      openAPIV3Schema:
        description: SelfUpgrade is the Schema for the selfupgrades API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SelfUpgradeSpec defines the desired state of SelfUpgrade.
            properties:
              components:
                description: Components defines which components to upgrade.
                items:
                  description: SelfUpgradeComponent defines a component to upgrade.
                  properties:
                    blocking:
                      description: Blocking indicates if this component must succeed
                        before continuing.
                      type: boolean
                    dependsOn:
                      description: DependsOn lists component dependencies.
                      items:
                        type: string
                      type: array
                    healthCheck:
                      description: HealthCheck defines the health check for this component.
                      properties:
                        endpoint:
                          type: string
                        labelSelector:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                        timeout:
                          type: string
                        type:
                          type: string
                      required:
                      - type
                      type: object
                    name:
                      description: Name is the component name.
                      type: string
                    order:
                      description: Order defines the upgrade order.
                      type: integer
                    type:
                      description: Type is the component type (deployment, crd, webhook,
                        rbac).
                      type: string
                  required:
                  - name
                  - type
                  type: object
                type: array
              paused:
                description: |-
                  Paused indicates whether the upgrade is paused.
                  When true, the controller will not proceed with the upgrade.
                type: boolean
              postUpgradeHooks:
                description: PostUpgradeHooks are hooks to run after upgrade.
                items:
                  description: Hook defines a pre/post upgrade hook.
                  properties:
                    command:
                      description: Command is the command to execute.
                      type: string
                    name:
                      description: Name is the hook name.
                      type: string
                    onFailure:
                      description: OnFailure defines the action to take on failure
                        (Abort, Continue, Retry).
                      type: string
                    timeout:
                      description: Timeout is the maximum time for the hook to complete.
                      type: string
                  required:
                  - command
                  - name
                  type: object
                type: array
              preUpgradeHooks:
                description: PreUpgradeHooks are hooks to run before upgrade.
                items:
                  description: Hook defines a pre/post upgrade hook.
                  properties:
                    command:
                      description: Command is the command to execute.
                      type: string
                    name:
                      description: Name is the hook name.
                      type: string
                    onFailure:
                      description: OnFailure defines the action to take on failure
                        (Abort, Continue, Retry).
                      type: string
                    timeout:
                      description: Timeout is the maximum time for the hook to complete.
                      type: string
                  required:
                  - command
                  - name
                  type: object
                type: array
              releaseImage:
                description: ReleaseImage is the OCI image reference containing upgrade
                  components.
                type: string
              strategy:
                description: Strategy defines the upgrade strategy.
                properties:
                  autoRollback:
                    description: AutoRollback enables automatic rollback on failure.
                    type: boolean
                  maxSurge:
                    description: MaxSurge is the maximum number of extra components
                      that can be created.
                    type: integer
                  maxUnavailable:
                    description: MaxUnavailable is the maximum number of components
                      that can be unavailable.
                    type: integer
                  minReadySeconds:
                    description: MinReadySeconds is the minimum time a component must
                      be ready before continuing.
                    type: integer
                  timeout:
                    description: Timeout is the maximum time for the entire upgrade.
                    type: string
                  type:
                    description: Type is the strategy type (Rolling, Recreate).
                    type: string
                type: object
              targetVersion:
                description: TargetVersion is the target version to upgrade to.
                type: string
            required:
            - targetVersion
            type: object
          status:
            description: SelfUpgradeStatus defines the observed state of SelfUpgrade.
            properties:
              completedTime:
                description: CompletedTime is when the upgrade completed.
                format: date-time
                type: string
              componentStatus:
                description: ComponentStatus tracks status of each component.
                items:
                  description: ComponentUpgradeStatus tracks the status of a single
                    component upgrade.
                  properties:
                    completedTime:
                      description: CompletedTime is when this component upgrade completed.
                      format: date-time
                      type: string
                    message:
                      description: Message contains any error or status message.
                      type: string
                    name:
                      description: Name is the component name.
                      type: string
                    phase:
                      description: Phase is the current phase of this component.
                      type: string
                    startedTime:
                      description: StartedTime is when this component upgrade started.
                      format: date-time
                      type: string
                    type:
                      description: Type is the component type.
                      type: string
                  required:
                  - name
                  - type
                  type: object
                type: array
              conditions:
                description: Conditions represents the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentVersion:
                description: CurrentVersion is the current version after upgrade.
                type: string
              history:
                description: History tracks upgrade history.
                items:
                  description: UpgradeHistory tracks upgrade history.
                  properties:
                    completionTime:
                      description: CompletionTime is when the upgrade completed.
                      format: date-time
                      type: string
                    message:
                      description: Message contains any error or status message.
                      type: string
                    startedTime:
                      description: StartedTime is when the upgrade started.
                      format: date-time
                      type: string
                    state:
                      description: State is the final state (Completed, Failed, RolledBack).
                      type: string
                    version:
                      description: Version is the target version.
                      type: string
                  required:
                  - startedTime
                  - state
                  - version
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the last generation observed by
                  the controller.
                format: int64
                type: integer
              phase:
                description: Phase is the current upgrade phase.
                type: string
              startedTime:
                description: StartedTime is when the upgrade started.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
